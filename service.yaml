apiVersion: v1
kind: Service
metadata:
  name: campusconnect-service
  namespace: campusconnect-ns
spec:
  selector:
    app: campusconnect
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
